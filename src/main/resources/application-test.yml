spring:
  datasource:
    url: jdbc:h2:mem:codeops_registry_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false
  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

codeops:
  jwt:
    secret: test-secret-key-minimum-32-characters-long-for-hs256-testing
  cors:
    allowed-origins: http://localhost:3000
  services:
    server-url: http://localhost:8090
    vault-url: http://localhost:8097
    logger-url: http://localhost:8098

logging:
  level:
    com.codeops.registry: WARN
